<?xml version="1.0" encoding="UTF-8"?>
<bean cache="device">
	<read name="id">
		select
		id,
		platform,
		token,
		user_id as "userId",
		channel_id as "channelId"
		from
		pns_device
		where
		id = ${this.id}
	</read>
	<read name="userId">
		select
		id
		from
		pns_device
		where
		user_id = ${this.userId}
	</read>
	<read name="token">
		select
		id
		from
		pns_device
		where
		token = ${this.token}
	</read>
	<execute name="disable">
		update pns_device
		set disabled = 1
		where
		token = ${this.token}
	</execute>
</bean>